#include <stdio.h>
int main(){
    int runs [3]= {97,91,35};
    for (int i=0;i<3;i++){
        printf("number %d is %d \n",i+1,runs[i]);
    }
    int runs1 [3];
    /* int j=0,i=2;

    while (j<3 && i>=0 ){
        runs1[j]=runs[i];
        j++;
        i--;
        } */
        int i,j;
    for (i){
        runs1[j]=runs[i];
    }
    printf("%d %d %d \n",runs1[0],runs1[1],runs1[2]);
    return 0;
}
